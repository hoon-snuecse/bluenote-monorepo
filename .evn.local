  # NextAuth
  NEXTAUTH_URL=http://localhost:3000
  NEXTAUTH_SECRET=[bluenote.site의 값 복사]

  # Google OAuth
  GOOGLE_CLIENT_ID=[bluenote.site의 값 복사]
  GOOGLE_CLIENT_SECRET=[bluenote.site의 값 복사]

  # Supabase
  SUPABASE_URL=[bluenote.site의 값 복사]
  SUPABASE_ANON_KEY=[bluenote.site의 값 복사]

  # Claude AI (있다면)
  ANTHROPIC_API_KEY=[bluenote.site의 값 복사]
