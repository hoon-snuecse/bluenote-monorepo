{
  "framework": "nextjs",
  "regions": ["icn1"],
  "buildCommand": "cd ../.. && pnpm install && cd apps/web && next build",
  "outputDirectory": ".next",
  "installCommand": "echo 'Install handled by buildCommand'",
  "env": {
    "NODE_VERSION": "20.x"
  }
}